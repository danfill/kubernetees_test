apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.shortApp.name }}-deployment
  namespace: {{ .Values.shortApp.namespace }}
spec:
  replicas: {{ .Values.shortApp.replicas }}
  selector:
    matchLabels:
      components: {{ .Values.shortApp.components }}
  template:
    metadata:
      labels:
        components: {{ .Values.shortApp.components }}
    spec:
      containers:
      - name: {{ .Values.shortApp.name }}
        image: "{{ .Values.short-app.image }}:{{ .Values.short-app.image_version }}"
        imagePullPolicy: Always
        resources:
          limits:
            memory: {{ .Values.shortApp.limits.memory }}
            cpu: {{ .Values.shortApp.limits.cpu }}
        ports:
          - containerPort: {{ .Values.shortApp.containerPort }}
