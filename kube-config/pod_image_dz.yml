apiVersion: v1
kind: Pod
metadata:
  name: image-app
  labels:
    components: web_image
spec:
  containers:
  - name: image-app
    image: antonlarichev/conv-app
    resources:
      limits:
        memory: "128Mi"
        cpu: "250m"
    ports:
      - containerPort: 80